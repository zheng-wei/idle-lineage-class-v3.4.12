# 原版差異分析報告 v3.1.67 → v3.3.15

- BASE(上次分析到): `de9129b79c2f25832c62d087e0bbc4c19659ec48`
- TARGET(本次分析到): `a3c8331b5a1664ec7a43925945779dfb0b49a2f4`
- 上游 27 個 commit,commit message 全是「1」,無法依賴,本報告全靠讀 diff。
- `js/`+`css/`+`index.html` 共約 10,300 行變動;`assets/` 有約 7.6 萬筆變動(動畫幀路徑大搬遷,多為同內容換路徑)。
- 上游自己不小心 commit 了 4 個 `.bak-*` 備份檔(`css/style.css.bak-*`、`js/13-shop-save.js.bak-*`),純雜物,已從分析中排除,**移植時不要碰**。

## 這次更新的最大主題

1. **全新「寵物系統 v2」**(`js/22-pets.js`,1178 行全新檔案)——取代舊的「項圈」系統,寵物變成有 HP/等級/技能的獨立實體,可誘捕/孵化/進化/裝備武器防具。
2. **全新「召喚獸系統 v2」**(`js/23-summons.js`,558 行全新檔案)——把召喚術/造屍術/屬性精靈從「無實體 buff」換成「有 HP、會被打死的多實體」。
3. **席琳套裝效果大改版**——從「直接掛在裝備上」改成獨立的 8 格「席琳遺骸」欄位,搭配新 NPC 伊奧(兌換)/菈克希絲(拆分)。
4. **城鎮 NPC 地圖化**——城鎮 NPC 從清單卡片改成地圖站位+8 方向 sprite 動畫。
5. **讀檔畫面重做**——4 格一頁、共 2 頁,**寫死只支援 8 個存檔位**。
6. **創角流程重做**——性別/職業分開選、立繪逐幀動畫。
7. 大量既有系統的平衡/bug 修正(BOSS 傷害自動平衡引擎、傭兵命中公式修正、CON 成長公式改分段等)。

**這 6 個主題彼此高度耦合、且大多牽動 5~9 支核心檔案,無法只挑一兩支檔案就移植完成。** 建議先決定「要不要做」這幾個大方向,再進入逐項移植。

---

## 🔴 需要使用者先拍板的重大決策項

### A. 寵物系統 v2 是否要整包移植(取代我方現有舊項圈系統)

**現況確認:我方 repo 目前仍是「舊項圈系統」**(上游這次要取代的前一代),不是無關的空白,存在明確命名衝突:

| 我方現有 | 上游新系統 | 衝突 |
|---|---|---|
| `PET_DEF`(js/01) | `PET_BOOK`(js/22,17 種型態+高等+黃金龍) | 概念重複 |
| `PET_STORAGE_MAX = 8`(js/12) | `PET_STORAGE_MAX = 20`(js/22) | **同名常數不同值**,後載入者覆蓋前者 |
| `player.petStorage`(道具陣列) | `_petRoster`(獨立 localStorage 桶,實體) | 資料模型完全不同 |
| `player.eq.pet`(裝備欄 slot `pet`) | 新增 slot `petwpn`/`petarm` | 新版內建 `petMigrateLegacy()` 遷移函式 |
| `renderPetStorageNPC()`(js/12,舊:項圈存提) | `renderPetStorageNPC()`(js/22,新:寵物列表) | **同名函式**,後載入者整個覆蓋前者 |

- 取得方式:誘捕道具(7 種,對應動物)+ 頑皮幼龍蛋孵化(50/50 兩種龍)。
- 戰鬥:出戰寵物(依魅力值決定隻數上限)獨立 tick、自動攻擊/吃藥/復活,貢獻 DPS 並分食玩家經驗。
- **工程量:大**。牽動 `00-data.js`(道具/怪物定義)、`01`(舊系統移除)、`03`(tick 掛點)、`04`(受害池)、`06`(healBeneficiaries)、`07`(施法閘)、`08`(裝備欄早退)、`10`/`13`(UI/存檔)、`12`(舊倉庫清單)十餘支檔案,加上約 2200+ 張新美術素材(`assets/anim/<寵物名>/d0~7/...`)。
- **與離線掛機的交互(需另外設計)**:寵物會自動攻擊/吃藥/死亡復活,但 `petsTick` 是逐 tick 呼叫、內部用逐拍計時器,與我方事件驅動快速結算架構衝突,**目前上游程式碼本身也沒有處理任何離線/補跑邏輯**,移植後若不額外處理,會出現「有寵物的角色離線掛機時寵物不出力」的落差,且要重踩一次「autoActions 只在 tick 路徑生效」「render 呼叫要包 `state.ff`」這些已知雷。
- **判斷:是否要做,是「要不要接受一次中型 feature 等級的工程」的決策,不是單純「順手加個功能」。**

### B. 召喚獸系統 v2(工程量比寵物略小,且不衝突我方傭兵偏離修改)

- 好消息:**不共用傭兵(ally)槽位/上限/buff 機制**,完全沒呼叫 `royalAllyMult()`,不會撞到我方「傭兵上限 7、王族加成停用」的偏離修改。
- 换血對象是我方**已存在**的 4 支技能(`sk_summon`/`sk_zombie`/`sk_elf_summon`/`sk_elf_summon2`),從「無實體 buff」整套換成「有 HP 多實體」——**是既有技能的玩法級改版,不是純新增**,需要使用者確認能接受這個轉變(含新 UI:隊伍面板顯示血量/倒數/重新施放鈕)。
- 沒有新增 localStorage key,召喚實體本身**明確不存檔**(存檔前後暫清、讀檔後靠自動重施重建),對我方「存檔安全」鐵則友善。
- **工程量:中高**——新檔本身可整檔搬,但要生效需同步修改 9 支既有核心檔的掛點(tick、受害池、healBeneficiaries、招募傭兵快照清欄位、buff 顯示、UI 面板、精通切換重施、存檔前後暫清)。
- **離線掛機需另外補**:上游程式碼完全沒有 `state.ff`/`fastAdvance` 相關處理,快速結算目前繞過 `tick()`,召喚物離線時不會攻擊/到期不會重施,需要額外設計。
- 需要新增少量美術素材(黑豹/巨大牛人/四種屬性精靈 8 方向幀,其餘多數重用既有怪物素材)。

### C. 席琳套裝效果「遺骸系統」重構 — ✅ 已於 2026-07-13 移植完成

> **實際做法與下面的原始評估不同**:上游的遺骸不是新資料結構,而是直接在 `player.eq` 加 8 個 slot(`rem_claw`…,slot 名＝物品 id),因此裝備/存檔/簽章全部沿用既有泛用機制。真正綁死 32 格網格的只有上游的 filler 補格邏輯,我方裝備分頁是純文字清單 → 直接在尾端加一段「🦴 席琳遺骸」8 列即可,反而更單純。
>
> **與上游的三處刻意偏離**(使用者拍板):① **保留** 40 級以上 BOSS 的賦予祝福卷軸掉落(我方仍在用碧恩賦予祝福);② 遺骸欄的三階發光走自寫 CSS(`.rem-lit`/`.rem-tier-N`),不用 Tailwind 任意值 class;③ 菈克希絲的部位對照多補了 `offwpn→之爪`、`shin→之肉` 兩個別名(我方 `sherineSetEligible` 含脛甲,不補會讓帶詞綴的脛甲永遠拆不掉)。

<details><summary>原始評估(移植前)</summary>

- 套裝詞綴不再直接掛裝備,改成 8 格獨立「遺骸」欄位(`rem_claw`/`rem_eye`/...),搭配新 NPC 伊奧(結晶兌換)、菈克希絲(裝備拆分)。
- **⚠️ 我方 2026-07-06 已把「1.8 八格框道具欄皮膚」整套移除**(`decorateClassicInventoryTab` 已刪,裝備分頁改成無背景純文字列表),而上游這個遺骸系統**完全建立在舊的 32 格網格版面上**——上游 patch 對我方目前的 `renderTabs` 完全套不上,**不是移植程式碼的問題,是要重新設計 UI 才能承載這個玩法**。
- 連動:席琳結晶掉率公式整個重寫(頭目/一般怪 × 等級的統一公式,取代舊的等級分段表),40 級以上 BOSS 移除「賦予祝福卷軸」掉落(**我方目前實際依賴這 3 種卷軸**,近期還在擴充碧恩賦予祝福的用途,若跟進需先確認上游是否有替代來源)。
- **建議:先讓使用者確認要不要這個玩法機制,若要,需抓「重新設計裝備分頁怎麼呈現遺骸欄位」當一個獨立子任務估工,而不是當作一般 patch 移植。**

</details>

### D. 城鎮 NPC 地圖化(純視覺大改,巨大但風險中等)

- NPC 從清單卡片改成地圖站位+8 方向 sprite 動畫,傲慢之塔/時空裂痕入口也改成地圖上的告示 NPC。
- 我方沒有動過相關函式內部邏輯,但這是自成一體的大視覺系統(新增約 300 行+大量 `assets/npc/` 素材),**建議整包移植或整包不做**,不要拆單一函式。
- 連動:城鎮背景改用 1920×1080 全景圖,且**城鎮安全區底圖被整段拿掉、改由地圖式 NPC 承載場景**——若只採用背景判斷邏輯而不做地圖式 NPC,城鎮畫面會背景空白,兩者要一起做或一起不做。

### E. 讀取角色畫面重做(與我方擴充的存檔格數直接衝突)

- 4 格一頁、2 頁,**寫死只支援 8 個存檔位**。**我方存檔格數已擴充到 16**(`save-slot-capacity.md`),直接套用只能顯示前 8 格、第 9~16 格完全看不到選不到。
- **若要採用,必須先重構分頁邏輯(4 格×4 頁),不是移植,是重新設計。**
- 需要對應 HTML/CSS(不在本次 diff 範圍)與立繪逐幀動畫素材。

### F. 創角流程重做(性別/職業分開選,立繪動畫)

- 我方沒改過創角流程本體,理論上可整段搬入,但需要對應 HTML 結構調整+大量新美術素材(`assets/start/<key>/N.png` 逐幀圖),且與上一項「讀檔畫面」耦合(`showCreation`/`backToMenu` 互通),**建議與 E 一起評估,不要分開做**。

---

## 🟢 低風險、可個別挑選採用的功能點(依檔案分組)

### `js/00-data.js`(純資料層,詳見下方總覽表)

- **BOSS 傷害/HP 自動平衡引擎**(`normalizeHighLevelBossHp`/`normalizeBossDamageByLevel`)——依等級自動拉高偏弱頭目的數值,不影響已夠強的頭目。低風險、可採用,但会重寫全體頭目數值,採用前建議先確認我方是否已手動調過同一批頭目。
- 鋼鐵瑪那魔杖客製製作(新 NPC + 2 把新武器)——獨立自足,**本次所有分析中風險最低、最適合優先做的項目之一**。
- 元素精靈召喚傷害公式調整(`dmgMult`/`mrPenBase`)。
- CON 成長公式改分段遞減(平衡調整,純函式替換)。
- 大量 relic(遺物)批次新增與描述文字精簡化——**我方先前已大量重複補過同一批 relic(201 個 key 重疊)**,價值偏低,建議跳過或最後才做。
- ⚠️ 20 幾支技能移除具名布林旗標改純文字說明——疑似戰鬥程式碼也同步改用技能 id 判斷,**不可只搬資料層**,需額外查證。
- ⚠️ `enhanceCap`/`maxEn` 與試煉道具掉落「遺物 ×2」加成——**與我方已改過的同一物理行衝突**(我方武器強化上限已改 20、`classicExemptDropMult` 重構),需手動合併,不能整行覆蓋。

### `js/01-drops-config.js` + `js/05-kill-progression.js`

- 乾淨可採用:炎魔之影孤兒 key 修正(bug fix)、掉落訊息顯示來源怪物、裂痕進場套背景圖、幼龍蛋更名。
- 平衡調整(獨立):CON 成長公式(見上)。
- ⚠️ 鋼鐵碎片材料鋪貨、兩批新遺物掉落——與我方已改過的怪物掉落行/`ITEM_WEIGHTS` 同行,需手動合併。
- 🔴 移除 40 級以上 BOSS 賦予祝福卷軸掉落——我方仍依賴此掉落來源,不建議照搬(見決策項 C)。
- 席琳遺骸系統/結晶掉率重寫——見決策項 C,整批跟或整批不跟。

### `js/02~04/06/07`(戰鬥/傭兵)

**低風險、建議優先採用的 bug 修正/平衡調整**:
- 傭兵命中判定改用 `stretchHitValue()` 軟地板曲線(修正高 AC 怪對傭兵命中系統性偏低)。
- `noAttack` 旗標實際生效(攻城塔/藏寶箱被打醒不會反擊——我方資料層已定義該欄位但邏輯層從未讀取,是個現成的既有 bug)。
- 傭兵仇恨權重數值微調(4→5、3→4)。
- 重擊骰面段擴充邏輯優先序修正、黑暗妖精會心一擊 MP=0 自殘 bug 修正、`manualCast` 補沉默/魔法封印閘、`allyTryPotion` 存檔堆疊誤刪 bug 修正。
- 約 10 項「傭兵機制對齊玩家」修正(魔力精通 MP 加倍/回魔、幻覺套裝套用範圍補齊、傭兵連射擴及所有職業、攻速改讀完整 `aspd`)——建議整批採用。
- 約 15 個新遺物效果(資料驅動,需搭配 `js/00-data.js` 對應資料,低風險)。

**🔴 高衝突(我方刻意偏離,以我方版本為準)**:
- `royalAllyMult`/`ALLY_ACTIVE_MAX`——我方已停用/改回舊平衡,上游持續原方向疊加,新功能移植時注意勿連帶改回。
- `_isMercSelfBuff`——雙方都改了同函式的不同語意(我方修 bug、上游開放幻術士召幻象自動維持),需手動合併不可整支覆蓋。

**⚠️ 需一併重新驗證的機制**:
- BOSS 每 5 秒緩速回血新機制——會改變 BOSS 戰鬥節奏,若採用需重新驗證我方離線掛機的「BOSS 懶驗證取樣耗時」邏輯。

### `js/08-items-equip.js` + `js/19-equipment-window.js`

- 低風險:裝備視窗 E 角標/堆疊數量/強化封頂顯示、掉落日誌「怪物名給你物品」。
- **🔴 與我方 `keepModal`/`silent` 偏離修改衝突**:聖伯納急救酒桶(藥水×2 特效)與我方 `potionHealBase` 隨機基準值改動落在同一小段落;寵物裝備欄早退邏輯與我方 `equipItem(item,silent)` 簽章需人工合併,**merge 時務必保留 `keepModal`/`silent` 判斷**。
- 黑暗妖精十字弓白名單移除(規則緊縮)——需確認我方物品庫對應武器 `req` 欄位是否要補 `dark`,避免玩家已裝備武器突然不可用。

### `js/10-ui-tabs.js` + `js/13-shop-save.js`

- **🔴🔴 `saveGame`/`loadGame` 核心流程改動,風險最高**:我方在完全相同的函式/相近行位已有大量掛點(離線掛機 `offlinePreLoad`/`offlineStamp`、`_lastSaveMs` 去重、`setTeleportBoss` 等),**絕對禁止整段覆蓋或 `git apply -3` 硬套,任何一項涉及存檔的功能都要先完整讀我方目前版本再動手**。
- **🔴 倉庴防複製「墓碑機制」**——設計思路值得參考(防止多分頁複製道具),但我方倉庫函式已大改(搜尋/捲動錨定/RELICDEX 分頁),不能整段貼上,只能照抄設計思路手動刻進我方版本。
- 低風險可採用:`buildItemDescHTML` 新增特效/屬性顯示欄位(附加性,不影響我方已改的 AC/mpOnHit 顯示)、AC 負值顯示 bug 修正、背包清單角標(裝備欄角標需等 UI 結構問題解決)、商店清單清理與雜項文案。
- 中風險:武器特效顯示「模糊化」(拿掉精確數字)——與我方「小百科要精確數據」原則方向相反,AC 修正建議單獨拆出來採用、模糊化不建議跟。

### `js/11-world-map.js` + `js/12-npc-quests.js` + `js/14-craft-pandora.js`

- **最適合優先做**:鋼鐵瑪那魔杖改造(神秘的魔法師 NPC)——獨立自足,只需確認/新增 2 個成品物品定義。
- **🔴 `changeMap` 城鎮版面規則調整**——我方 `offlineStamp()` 掛在同函式尾端,雖不逐字重疊仍需人工逐行合併,不建議自動套用。
- **🔴 屬性精靈精通切換收回/昇華**——我方 `chooseMastery` 已拆成確認彈窗+`_applyMastery` 兩段,插入點需對應調整,且依賴召喚獸 v2(決策項 B)。
- 席琳遺骸系統的 NPC 端(伊奧/菈克希絲)、寵物防具鍛造——依賴決策項 C/A,不宜半套跟進。
- 低風險小修正:技能 tooltip 補強、其他瑣碎防禦性寫法修正。
- **離線掛機檢查結果:本次範圍沒有新增「非選單地圖」,`changeMap` 核心流程(`mapState.current`/`syncMapSelectors`)未受影響**,`offlineStamp()` 掛點理論上安全,但仍建議人工複查一遍。

### `js/09-vfx-render.js` + `js/15~18`(視覺/音效/圖鑑)

- **🔴 不建議搬**:`js/18-misc-book.js` 下架賦予祝福卷軸——我方仍在用且近期才擴充用途,照搬會讓玩家正在用的卷軸從收集冊消失。
- **建議優先採用**:`state.ff` 靜音範圍擴大到 `vfxKill()`/`_mobAnimTrigger()`——我方目前這兩處**完全沒有 `state.ff` 檔板**,離線快速結算逐殺呼叫 `vfxKill` 時會做整套多餘 DOM 操作,是我方 CLAUDE.md 已記錄過的同類雷的翻版,補兩行 `if(state.ff)return` 即可,值得優先做並實測。
- 低風險可採用:圖層顯示保護(`onerror` 改用 `visibility`)+ 怪物名稱 tooltip、創角動畫音效/BGM(`setSfxOn` 與我方版本有同行衝突需手動合併)。
- 中風險:裝備收集冊分類修正(`petarm`/`petwpn`/`quiver`)——與我方已加的 `shin`/`tshirt` 分類是「相鄰插入」需手動合併,`quiver` 依賴遺物架構與我方(遺物已整批移到獨立收集冊)方向不同,建議跳過 `quiver`、只挑 `petarm`/`petwpn`(若移植寵物系統的話)。
- 弓箭投射物 sprite、八方向怪物動畫——獨立子系統,建議整包評估,依賴掛點檔案(js/03/04/06)不在本次比對範圍。
- **🔴 高風險**:職業動畫三方向(`domKey`)——`_playerMorphApply`/`_allySpritesApply` 我方已大改(`_PM_ATTACK_SKILLS`/`_pmAbsorbSelfHpCost`),需手動合併,且需確認我方是否已有全職業三方向素材。
- **特別確認結果:本次 diff 沒有新增任何會自我扣血的新技能,也沒改動受擊/施法的 HP-delta 判斷邏輯,`_PM_ATTACK_SKILLS`/`_pmAbsorbSelfHpCost` 排除清單這次不需要更新。**
- **⚠️ 前提不成立、不建議跟**:特效尺寸校正/怪物垂直散佈/攻城建築固定站位——建立在上游「16:9 戰鬥框改版」上,我方戰鬥框仍是使用者要求改回的窄條式(`aspect-ratio:1920/580`),沒有跟進 16:9,套用這些校正邏輯等於在錯誤前提上校正。

---

## 移植狀態總表

| 功能 | 規模 | 衝突風險 | 狀態 |
|---|---|---|---|
| A. 寵物系統 v2(整包) | 大 | 高(命名/架構衝突) | ✅ 已移植 @9cc8e66f |
| B. 召喚獸系統 v2(整包) | 中高 | 中(需補離線相容) | ✅ 已移植 @58f493bb |
| C. 席琳遺骸系統(整包) | 中大 | 高(UI 結構不相容) | ✅ **已移植(2026-07-13)** |
| D. 城鎮 NPC 地圖化(整包) | 巨大 | 中 | ✗ 不做（使用者決定） |
| E. 讀取角色畫面重做 | 大 | 高(8 格 vs 我方 16 格) | ✗ 不做（我方 16 格存檔） |
| F. 創角流程重做 | 大 | 中 | ✗ 不做（只取立繪動畫＋語音） |
| BOSS 傷害/HP 自動平衡引擎 | 中 | 低 | 未移植 |
| 鋼鐵瑪那魔杖客製製作 | 中 | 低(優先候選) | ✅ 已移植 @9b7c48dc |
| 元素精靈召喚傷害公式調整 | 小 | 低 | 未移植 |
| CON 成長公式分段遞減 | 小 | 低 | 未移植 |
| relic 批次新增/描述精簡化 | 大 | 中(價值偏低) | ✅ 已移植 @5bce6249 |
| 技能布林旗標→純文字重構 | 中 | 中(需查證戰鬥碼) | 未移植 |
| `enhanceCap`/`maxEn` + 遺物×2掉落加成 | 小 | 高(同行衝突) | 未移植 |
| 掉落/擊殺小修正(炎魔之影/掉落訊息/裂痕背景/幼龍蛋更名) | 小 | 低 | ✅ 已移植 @566aa09d（幼龍蛋隨寵物 v2） |
| 移除 BOSS 賦予祝福卷軸掉落 | 小 | 高(我方仍依賴) | 不建議跟 |
| 傭兵命中軟地板曲線修正 | 中 | 低(優先候選) | ✅ 已移植 @441595c1 |
| `noAttack` 旗標生效修正 | 極小 | 低(優先候選) | ✅ 已移植 @9b7c48dc |
| 傭兵仇恨權重數值微調 | 小 | 低 | ✅ 已移植 @51a999ae |
| 傭兵機制對齊玩家(約10項) | 中 | 低 | ✅ 已移植 @441595c1+e551e58b+2d57831b |
| 新遺物效果(約15個) | 中 | 低 | ✅ 已移植 @5bce6249 |
| `royalAllyMult`/`ALLY_ACTIVE_MAX` | — | — | 以我方版本為準,不跟 |
| `_isMercSelfBuff` 幻覺自動維持 | 中 | 中高 | ✅ 已移植 @2d743373 |
| BOSS 每5秒緩速回血 | 中 | 低中(需重驗離線取樣) | ✅ 已移植 @2f004658 |
| 裝備視窗角標/掉落日誌來源怪物 | 小 | 低 | ✅ 已移植 @c8ff856e+566aa09d |
| 聖伯納急救酒桶(藥水x2) | 小 | 高(同段落衝突) | ✅ 已移植（隨遺物批次） |
| 黑暗妖精十字弓白名單移除 | 小 | 低(需查裝備req) | ✅ 已移植 @d984bed3 |
| `saveGame`/`loadGame` 核心流程改動 | 大 | **極高** | ✅ 已評估，取「寫入失敗處理」@d5c66488 |
| 倉庫墓碑防複製機制 | 大 | 極高 | ✅ 已移植 @d5c66488 |
| `buildItemDescHTML` 新增顯示欄位 + AC修正 | 中 | 低 | ✅ 已移植 @e4f72b22 |
| 武器特效顯示模糊化 | 小 | 中(與小百科精確原則相反) | 不建議跟 |
| 商店清單清理/雜項文案 | 小 | 低 | ✅ 已移植（肉下架隨寵物 v2） |
| 技能 tooltip 補強 | 小 | 低中 | ✅ 已移植 @c8ff856e |
| `misc-book` 下架賦予祝福卷軸 | — | 高(我方仍用) | 不跟 |
| `state.ff` 靜音擴大(vfxKill/_mobAnimTrigger) | 小 | 低(優先候選) | ✅ 已移植 @9b7c48dc |
| 圖層顯示保護 + 名稱tooltip | 小 | 低 | ✅ 已移植 @b8c2389f |
| 裝備收集冊分類修正(petarm/petwpn) | 小 | 中 | ✅ 已移植（隨寵物 v2） |
| 創角動畫音效/BGM | 中 | 中(setSfxOn同行) | ✅ 已移植 @6d332161 |
| 弓箭投射物sprite / 八方向怪物動畫 / 職業動畫三方向 | 大 | 中～高 | ✅ 已移植 @91878108 |
| 特效尺寸校正等(16:9相關) | 中 | 前提不成立 | 不建議跟 |

## 建議優先順序(若要挑幾項先做)

1. **鋼鐵瑪那魔杖客製製作**(js/11/14)——全風險最低、獨立自足。
2. **`state.ff` 靜音擴大到 `vfxKill`/`_mobAnimTrigger`**(js/09)——可能是既有效能/畫面洩漏 bug,順手修。
3. **`noAttack` 旗標生效修正**(js/03)——一行修好一個現成 bug。
4. **傭兵命中軟地板曲線修正**(js/06)——平衡性 bug 修正,無架構衝突。
5. 其餘「傭兵機制對齊」約 10 項小修正——可整批評估採用。
6. A/B/C/D/E/F 六個大方向決策項——需要使用者先拍板方向,再各自估工分階段做。
