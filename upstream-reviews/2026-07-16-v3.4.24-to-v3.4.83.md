# 上游差異分析報告：v3.4.24 → v3.4.83

- **分析區間**：上游 `869445e5` → `f0c9a4ef`（27 個 commit，`js/` 1693 行新增）
- **上游版本**：v3.4.24 → v3.4.83
- **分析日期**：2026-07-16（UTC+8）
- **我方錨點**：HEAD `699ec4e7`（分家點 divergedAtOurs `58ad5809`）
- **移植狀態欄**：未移植 / 已移植@commit / 使用者略過 — 之後想補加翻本報告即可，不用重 diff

---

## 📊 移植進度總表（2026-07-16 執行·以此表為準，下方各節的舊「移植狀態」行已過時）

| 項目 | 狀態 | commit |
|---|---|---|
| 🔴 大整包 A 方向魔法/治癒/幻覺公式重寫 | ✅ 已移植（使用者裁定「連平衡也翻成原版方向」） | `96073cee` |
| #1 席琳 MR 上限修正 | ✅ 已移植 | `c86a44d7` |
| #2 聖使阿卡塔死亡經驗買回 | ✅ 已移植（我方無 NPC 坐像系統→接卡片渲染器，2141 素材不移植） | `bcee4f4c` |
| #3 新怪掉落一批（遺物16/17批·倫得雙刀等） | ✅ 已移植 | `fb845c5f` |
| #4 冥皇執行劍解咒版＋兩變身 | ✅ 已移植 | `fb845c5f` |
| #5 污濁之水 muddywater | ✅ 已移植 | `a1362d70` |
| #6 頭目 HP 自然恢復砍半 | ✅ 已移植 | `65acda1e` |
| #7 經驗表 Lv70-99 重製 | ⬜ 使用者未選 | — |
| #8 動畫載入加速（平行探測+manifest） | ✅ 已移植（manifest 以我方 assets 自產） | `25e69021` |
| #9 死亡動畫可靠性 | ✅ 已移植 | `495858a8` |
| #10 `state.ffSmall`（`_tickDebt` 我方設計本就免疫該 bug） | ✅ 已移植 | `65acda1e` |
| #11 寵物 v2 後續（精通重做/outOwner） | ✅ 已移植 | `9a57b503` |
| #12 傭兵召喚物戰鬥實體 | ⬜ 未做 | — |
| #13 NPC 視覺雜項 | ⬜ 使用者未選 | — |
| #14 零散新特效/新武器欄位 | ✅ 已移植 | `2736964d` |
| #15 其他小修 | ✅ 已移植（柴刀已是新值·第15批遺物我方未移植故略） | `758ea420` |
| #16 純美術（區域背景/5怪動畫） | ✅ 已移植（area 扁平化對名·只覆蓋我方既有58張） | `a3fd46e1` |
| 原作者標記/官方版指引機制 | ✅ **已保留**（使用者明訂·尊重原作） | `e466f3e5` |

**收尾待辦**：離線快速結算 A/B 重校（公式改了殺速）、`/update-wiki` 更新小百科/掉落查詢、`/prepush`（bump `?v=`＋stamp-sw-version＋gen-manifests＋smoke）→ push。

> ⚠️ 上游 commit message 全是「1」不可依賴，本報告一律讀 diff 本身得出。
> ⚠️ 我方分家後幾乎每支核心 js 都自己改過，**所有移植一律 3-way**。

---

## 🚦 一句話總覽（決策先看這段）

這 60 個版本的內容可以歸成三類：

1. **🔴 一個「方向魔法公式重寫」大整包（閘門級決策）** — 橫跨 `00/01/02/03/04/06/07` 七支檔，把所有攻擊魔法/治癒/幻覺套裝的算法整個換掉（魔傷改吃「屬性防禦係數 + SP 封頂」、治癒改只吃 INT、AOE 回魔削弱）。**這是產品級平衡決策，不是純技術移植**：要嘛整套一起搬、要嘛整套不搬，單搬半套會 `ReferenceError` 害離線結算歸零。**下面所有標「🔴依賴公式整包」的項目都吊在這個決定上。**

2. **🟢 一批可獨立挑的新內容/新特效** — 新遺物（十六、十七批約 24 件）、冥皇執行劍解咒變身、倫得雙刀、聖使阿卡塔死亡買回 NPC、污濁之水、寵物精通重做、傭兵召喚物實體化、動畫載入加速…等。多數是「新資料 + 新掛點」，可逐項評估，但常需連 assets 一起搬。

3. **🚫 兩項絕不移植** — 原作者防盜橫幅/浮水印（會讓我方站台自己跳「非官方轉載」），及上游 index.html 的版本號/anim-manifest.js 架構（我方走自己的 stamp/manifest/外掛架構）。

**另有一項「我方已自行收斂到相同結論」**：👑 王族改制（停用 `royalAllyMult`、傭兵上限 7）— 我方 2026-07-08 就改成跟上游這次一模一樣，**維持現狀、不需移植**（`allySlotList` 必須保留我方 `SAVE_SLOT_MAX` 版本，上游硬寫死 8）。

---

## 🔴 大整包 A：方向魔法/治癒/幻覺公式重寫（閘門級決策）

**玩家視角**：所有攻擊法術數值重新平衡；怪物的火抗/水抗等元素抗性開始真正生效；治癒術改成一次補全隊、且治癒量只看 INT（治癒法師會變弱）；幻覺套裝 AOE 回魔被削（續航變合理）；SP（魔法威力）有封頂 33/32。

- **涉及檔/函式**：
  - `00-data`：幾乎所有攻擊魔法 `sk_*` 改骰值 + 新欄位 `dmgBase`/`apexAoe`/`apexSingle`；治癒技能加 `classicHeal`/`groupHeal`/`healCooldownTicks`；`sk_regen`/`sk_elf_lifebless` 由 HoT 改即時全隊。
  - `01-drops`：`TEAM_AURA_SKILLS` 改內容、新增 `TEAM_SHARE_BUFFS` Set。
  - `02/03/04/06/07`：新 helper `magicBaseDamage`/`magicDamageCoef`/`weaponMagicDamageCoef`/`magicAttrDefense`/`rollHealingSpell`/`physicalHitSoftFloor`/`consumeWetMult`/`getIntExtraMp`/`shareTeamBuffs`；舊係數 `(1+3×magicDmg/16)` 全面替換；`illusionMagicDmg` 加 `recoverMp` 參數。
- **assets**：無（純公式；新技能圖示另計）。
- **衝突風險**：**極高**。我方 repo **完全沒有**這批 helper，代表我方停在此改動之前的舊公式；上游把它們散在七支檔、我方那些函式又各自改過（法師階級加成我方已移除、傭兵自我 buff 我方已修）。無法 `git apply`，得逐函式手動替換。**且直接改殺速 → 移植後離線快速結算 svcPerKill 全要重新校準（強制 A/B）**。
- **子項（若決定走整包，這些是同一批，不可只挑半套）**：方向魔傷公式、治癒重做(`rollHealingSpell`)、幻覺套裝 MP 語意、`shareTeamBuffs`(單體輔助全隊共享)、`physicalHitSoftFloor`(命中軟下限)、DoT 歸因(`_dpsCreditDot`，純 DPS 顯示、無玩家可見價值)。

> **決策點**：要不要讓我方跟進「原版方向魔法公式」？
> - **要** → 排一個獨立大工程，七檔一起 3-way + 離線重校 + 全套 `/update-wiki`。
> - **不要** → 下面標「🔴依賴公式整包」的全部略過。

**移植狀態**：未移植（待使用者決定走不走）

---

## 🟢 可獨立評估的新內容 / 新功能

### 1. 席琳世界 MR 上限修正（高魔防頭目不再被 ×3 壓成近乎魔免）
- 玩家視角：瘋狂席琳的怪物魔防成長改「原始MR + min(原始MR,200)」、一般席琳 ×1.5，改善打高防頭目手感。
- 涉及：`05-kill-progression` `applySherineBuff()`（`_m.mr` 單行公式）+ `11-world-map` 瘋狂席琳說明文字。
- assets：無。衝突風險：**低**（我方席琳 AC 固定值已與上游一致，只差 MR 這行，3-way 乾淨）。
- **最划算的低風險項**。移植狀態：未移植

### 2. 聖使阿卡塔 — 死亡經驗買回 NPC（經典模式限定·亞丁）
- 玩家視角：經典模式死掉可花錢向 NPC 買回一半損失的經驗。
- 涉及：`00-data` `npc_arkata`(`classicOnly`)、`11-world-map` `renderArkataBuyback`/`arkataBuyback`/`interactNPC` 分支/`classicOnly` 過濾/`TOWN_NPC_SPOTS.town_aden`、`04` `player.deathLog`(killPlayer 記錄)。
- assets：**有**，NPC 坐像 `assets/npc/2141/`（約 13 檔）。
- 衝突風險：**中**（新函式可整段貼，但 interactNPC/NPC_SPR/TOWN_NPC_SPOTS 我方都動過需逐一插；要連 04 deathLog + 00-data NPC 定義一起才點得出來）。移植狀態：未移植

### 3. 新怪掉落一批（倫得雙刀 / 冥皇執行劍解咒版 / 兩本魔法書 / 精鋼碎片 / 遺物第16-17批）
- 玩家視角：一批新武器與遺物加進各怪掉落表。
- 涉及：`01-drops` 多段 `MOB_DROPS.push` + `ITEM_WEIGHTS`；item 定義在 `00-data`。
- assets：**有**，各武器/書/遺物 icon（多為無 `img` 欄位、走中文名組路徑，需對應 PNG）。
- 衝突風險：**低~中**（表尾 append 型）。⚠️ 遺物本體效果靠 `02/03/04/06/07/22` 掛點，**單搬掉落表 = 隱形/半殘裝備**。移植後必跑 `/update-wiki`（掉落查詢五表）。移植狀態：未移植

### 4. 冥皇執行劍（解咒版）+ 兩個變身動畫
- 玩家視角：裝備後變身「真死亡騎士 冥皇丹特斯」、攻擊觸發大地崩裂、對地/風敵人普攻 ×1.4 的傳說遺物。
- 涉及：`00-data` `wpn_uncursed_emperor_blade`(`counterEles`/`procSkill:sk_earth_collapse`/`ignHardSkin`)、`02` `_setPoly`/`POLY_TIERS`/`DANTES_POLY_FORM` 變身表 + `pf.apm` 逐武器攻速、`09` `MORPH_BATTLE_ANIM`、`17` `MORPH_SKILL_SFX`(借死亡騎士音效)、`19` `MORPH_ANIM_PORTRAIT`、`10` weapon tag。
- assets：**有，約 192 檔** `assets/morphanim/`（2 變身資料夾）+ 武器/技能 icon + `assets/fx/大地崩裂`（約 12 檔）。
- 衝突風險：**中高**（跨 6+ 檔；`counterEles` 傷害倍率吊在公式相關檔；缺變身素材則隱形）。移植狀態：未移植

### 5. 污濁之水 muddywater（頭目限定技，頭目回血減半）
- 玩家視角：對頭目施放後，頭目每 5 秒自我回血再減半，更好磨死。
- 涉及：`06` `newMobStatus`/`STATUS_NAME`/`processMobStatusTick`、`03` 回血減半讀取、`07` `bossOnly` 過濾、`00-data` 技能 `sk_elf_muddywater` + 技能書 `bk_elf_muddywater`、`09` 狀態徽章 + `assets/fx/污濁之水`。
- assets：**有**（fx 約 13 檔 + 技能/道具 icon）。衝突風險：**低**（新增獨立狀態 kind，掛點乾淨）。移植狀態：未移植

### 6. 頭目 HP 自然恢復砍半（1%/5% → 0.5%/2.5%）
- 玩家視角：所有頭目每 5 秒自我回血量直接減半（對玩家有利）。
- 涉及：`03` tick 頭目 regen 區塊單點數值。assets：無。衝突風險：**中**（純數值，輕微影響離線純 BOSS 圖殺速）。移植狀態：未移植

### 7. 等級經驗表 Lv70-99 重製（高等升級不再天價）
- 玩家視角：Lv99→100 從約 3.74 兆降到約 38 億；舊角色載入時按比例換算保留進度。
- 涉及：`00-data` `EXP_REQ_CLASSIC` 以「Lv69 擊殺比例」為錨重算 + `_expReqClassicV2` 遷移函式、`13-shop-save` `loadGame` `expMigV=3` 舊存檔百分比遷移。
- assets：無。衝突風險：**中**（公式我方未動、低；但 13 的 loadGame 我方插了離線掛點，遷移區塊需 3-way + 驗離線）。⚠️ 一定要連 13 的遷移一起搬，否則老玩家等級進度會亂。移植狀態：未移植

### 8. 動畫首次載入大幅加速（平行幀探測 + anim-manifest 查表）
- 玩家視角：高延遲環境下怪物/寵物動畫首次載入時間約縮到 1/6。
- 涉及：`09` `_probeFramesWin`(一次並行 6 張)/`_manifestCount`，重寫四支 `probeSeq`；新檔 `js/anim-manifest.js` + 工具 `tools/gen-anim-manifest.js`；`22` `_pet8Probe` 共用。
- assets：無（但需引入新生成檔並重跑）。
- 衝突風險：**高**（落在 09 最擁擠區、我方 09 重度偏離 +384/-234 含 ff 早退/`_pmAbsorbSelfHpCost`）。移植後**必重驗離線 ff 洩漏**。⚠️ 這是「幀數查表」，與我方 SW 圖桶對帳 `anim-manifest.json` 是兩回事，別混淆。可獨立成一 commit 最後做。移植狀態：未移植

### 9. 死亡動畫可靠性修正（群體連殺不再「有時不播」）
- 玩家視角：一次清一片怪時，擊殺特效/死亡殘影不再被跳字或法術粒子洪水誤擋掉。
- 涉及：`09` `vfxKill` 用獨立 `_deathGhostCount`(上限12) 取代 `childElementCount<150`；加「死亡幀未就緒→用最後可見幀淡出」保底；`state.ffSmall`(小補跑放行殘影)依賴核心新旗標。
- assets：無。衝突風險：**中高**（同 09 檔；ffSmall 需核心引擎/離線補跑模組同步引入）。移植狀態：未移植

### 10. `_tickDebt` 進 finally（補跑可靠性 bug 修正）
- 玩家視角：修一個會讓遊戲時間追不平、畫面卡住的隱性 bug（例外時已跑的 tick 也要扣，否則 owed 只增不減、每輪重跑巨量補跑再拋例外）。
- 涉及：`03` gameLoop `_tickDebt -= ran*TICK_MS` 從 finally 外移入內；`state.ffSmall` 旗標。
- assets：無。衝突風險：**中**（gameLoop 我方也改過 `_dps.ms`/flushAwaySummary）。**這是真正值得移植的可靠性修正，與我方離線/背景補跑直接相關**，優先評估。移植狀態：未移植

### 11. 寵物系統 v2 之後的新變更（我方已移植 v2，以下皆為新增）
- **11a 王族「夥伴精通」k_royal_pet 重做**：寵物傷害×1.5/命中×1.5/受傷−50%，移除舊 `petMasteryMagicBonus`（我方仍在用）與魅力係數。衝突：**中**（需手動拆我方舊精通模型，否則新舊雙算）。
- **11b 遺物寵物裝備 3 件**：`petArmorDmgReduceMult`(寵物專用盔甲)、`petBleed`(小惡魔尖牙套·寵物普攻出血)、`autoReviveScroll` 耳環(巨靈的承諾·跳復活冷卻)。衝突：**低**（依賴 DB item 欄位）。
- **11c 多帳號/多分頁寵物歸屬修正**：出戰歸屬由格號 `outSlot` 改角色 ID `outOwner`(`char:<enSeed>`)，修「不同帳號用同一格→王族寵物被低魅力角色自動收回」；含 `petMigrateExpReqV3`。衝突：**中**（改動面廣但我方 22 偏離低，3-way 大致可套，需驗存檔遷移不誤收寵物）。
- assets：**有**（寵物裝備 icon）。移植狀態：未移植

### 12. 傭兵召喚物「戰鬥實體」（有血、可被打死/被治癒/顯示血條）
- 玩家視角：傭兵召喚出的東西變成有血量的實體，會被怪攻擊、也吃全隊治癒，隊伍面板顯示血條。
- 涉及：`23` `mercSummonList`/`renderMercSummonTeamHTML`/`summonTeamSignature`、`06` `_mercSummonAttachEntity`、`04` 受害者池、`08` `renderMercSummonTeamHTML` 接顯示。
- assets：無（註解稱無 sprite 在場、只有受擊判定）。衝突風險：**低**（我方 23 幾乎與上游 base 相同），但**綁 06+04 屬一整個子系統需成套**，單移 23 只是空殼。移植狀態：未移植

### 13. NPC 相關視覺/資料雜項
- **13a NPC 互動視窗表頭重構**：關閉鈕文字「返回村莊」→「關閉」，長 NPC 名不再擠出框。涉及 `index.html` `#town-interaction-header` 系列 + `css`。衝突：**中**（純結構/樣式，需手動對位）。
- **13b 倉庫 NPC 外觀翻新**（朵琳新造型、舊寶箱侏儒退役）：`11` NPC_SPR 表 + `assets/npc/10669`(7檔)、刪 `assets/npc/54`。衝突：**中**（美術微調，玩家幾乎無感，可略過）。
- **13c 屬性抗性顯示改數值**（去掉 %）：`index.html` + `08` `_updateUIImpl`。衝突：**中**（兩檔一起）。
- **13d 血盟盟主立繪改具名 CSS class**：`12` `renderPledgeNPC` + `css` `.pledge-leader-*`。衝突：**中**（⚠️ 我方用 purge 過的 tailwind-built.css，改 inline class 需把三個 class 寫進 style.css 否則版面壞）。**建議略過**除非 css 那批一起評估。
- 移植狀態：未移植

### 14. 零散新特效/新武器欄位（多吊在公式相關檔，個別可挑）
- 海洋水晶球 `onHitWet`/`consumeWetMult`(潮濕→風傷×2)、鬥士決戰服 `critDmgLowHp`、復仇者十字弩弓 `rapidMax`、骸骨意志之弓 `bonespike`、長老雷電 `procOnHit`(命中才觸發)、笨重鋼鐵石盾 `noEvade`、召喚儀式魔術布 `summonCtrl`、法師護身短刀 `castOnHurt`(受傷20%免費施法)、施法材料 `costItem`、烈焰巫師長袍 `fireballBurst`、巨魔再生戒指 `hpRegenFaster`。
- 多為「新欄位 + 既有結算點加一行判斷」，個別可挑；但傭兵端掛點常夾在被公式重寫的函式裡（單挑會被公式 hunk 卡）。移植狀態：未移植

### 15. 其他小修
- `wpn_7` 柴刀變強(dmgS 4→10/dmgL 11→17)、多把傳說武器 spellProc 骰值上修+補 `flat`、第十五批遺物補 `relicRole` 文案、`sk_elf_steelguard` 鋼鐵防護改回單體 AC-10、王族魔法 `teamAura`(灼熱武器/閃亮之盾全隊共享)、大地屏障傭兵鏡像、「掛機期間獲得」訊息只在真正切背景才顯示(⚠️ 需先確認與我方 `js/offline.js` 相關性、可能是死碼)。移植狀態：未移植

### 16. Assets 純美術更新（無程式）
- `assets/area/` 全區域背景 1920×1080 重新輸出（M 156 檔，畫質/壓縮更新）、`assets/anim/` 5 隻怪重轉（地/火/風元素守護者、污染的地精靈、漂浮之眼，M 129 檔）。純換圖，可獨立搬（搬後跑 `gen-manifests.mjs` 重產對帳）。移植狀態：未移植

---

## ✅ 原作者標記機制 — 已保留（2026-07-16 使用者明訂，推翻本報告初版的「絕不移植」）

- **原作者標記 + 官方版指引橫幅 + 浮水印 canary**（`00-data` 檔尾約 60 行 `_origEnforce`/`_origAuthorizedHost`/`ORIG-shines871-…`，`03` gameLoop 開頭重掛）。
- **本報告初版判定為「絕不移植」（理由：會讓我方站台自己跳「非官方轉載」橫幅），使用者於 2026-07-16 明確要求保留，理由是「對原作的尊重」——此決定優先。**
- 事實澄清：原作者**開放非商業轉載（須標示出處）、僅禁商業營利**，橫幅措辭刻意中性（無「盜版/未授權」等指控），只是告知「這是非官方轉載版、官方最新免費版在此」。我方為非商業鏡像且已標註原作者，完全符合其授權條件；橫幅在我方站顯示是**預期且正確**的行為。
- **原作者程式碼一字未改**。放行條件：官方站/localhost/127.0.0.1/file:// 皆放行（例外亦放行，不誤傷）。已實測：本機不誤觸、非官方 host 正常顯示、橫幅被移除後 gameLoop 自動復原、0 JS error。
- ⚠️ **日後移植任何檔案時，不可再剔除 `_origEnforce` 呼叫點或 canary。**

## 🚫 仍不移植

- **上游 index.html 版本號 / 去 BOM**：我方走自己的 stamp/外掛架構，一律不照搬。（上游的 `anim-manifest.js` 引用已於 #8 以我方自產版本納入。）

---

## 📋 建議優先序（給挑選參考）

| 優先 | 項目 | 理由 | 風險 |
|---|---|---|---|
| ⭐ 高 | #10 `_tickDebt` finally 修正 | 真可靠性 bug、與我方離線/補跑直接相關 | 中 |
| ⭐ 高 | #1 席琳 MR 上限修正 | 低風險、玩家有感、單行 | 低 |
| 高 | #5 污濁之水 / #6 頭目回血砍半 | 掛點乾淨、玩家有感 | 低~中 |
| 中 | #2 聖使阿卡塔 / #3 新怪掉落 / #4 冥皇劍變身 / #7 經驗表 | 玩家有感的新內容，但跨檔 + 需 assets/遷移 | 中 |
| 中 | #11 寵物新變更 / #12 傭兵召喚實體 | 子系統成套 | 中 |
| 低 | #8 平行探測 / #9 死亡動畫可靠性 | 體感/穩定，落在 09 擁擠區 | 中高 |
| 低 | #13 NPC 視覺 / #16 純美術 | 可有可無 | 低~中 |
| 🔴 決策 | **大整包 A 方向魔法公式** | **產品級平衡決策，先拍板走不走** | 極高 |
| 🚫 | 王族改制（已收斂）/ 防盜橫幅 / index 架構 | 見上 | — |

**共同前置提醒**：#3/#4/#14 多需 `00-data` 新增裝備/技能欄位 + 對應 icons；`weaponSpellProc`/`illusionMagicDmg` 等簽章改會連動 js/06 傭兵鏡像，需同批處理。任何 06/13/index.html 的 3-way 都會在我方刻意偏離處爆衝突標記，**必須逐 hunk 人工確認、不可盲目 `git add -A`**。
