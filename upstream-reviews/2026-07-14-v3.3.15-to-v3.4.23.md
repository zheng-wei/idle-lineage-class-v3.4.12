# 原版差異分析報告 v3.3.15 → v3.4.23

- BASE(上次分析到): `a3c8331b5a1664ec7a43925945779dfb0b49a2f4`
- TARGET(本次分析到): `869445e5c50e9aa0f3093eaf567ac62b2b6f508f`
- 上游 13 個 commit,commit message 全是「1」,無法依賴,本報告全靠讀 diff(含修改既有行的 `-`/`+` 成對)。
- `js/`+`css/`+`index.html` 共約 2,441 增 / 508 刪,橫跨 21 支檔;`assets/`+`public/` 約 970 檔變動。

## 這次更新的最大主題

1. **黑暗妖精聖地整包**(新安全區 + 3 張隱藏圖 + 9 隻新怪 + 2 隻 Lv99 BOSS + 約 25 件新物品)——占整批一半以上,跨 10 支檔。
2. **我方剛移植的寵物 v2 / 召喚獸 v2 的後續修正與平衡**——上游在我們移植之後又改了一輪,含玩家已回報的 bug。
3. 潘朵拉權重規則重寫、裝備視窗改成分頁內嵌、能力頁重做、遺物第 15 批。

> ⚠️ **我方特別處境**:js/22-pets.js 與 js/23-summons.js 是 2026-07-14 才移植進來的,上游在 BASE 之後又改了 132 / 139 行。**這批「後續修正」不跟的話,玩家會踩到上游已經修掉的 bug**(例如寵物「黃金龍自己不見了」)。

---

## 🟢 第 1 組:寵物 / 召喚獸 v2 的後續修正(強烈建議優先做)

### 1-1. 寵物平衡:整體增傷 + 命中補強
- **玩家視角**:寵物普遍變強——一般寵平均總傷約 +20%、高等寵約 +50%,全寵命中 +5(比較少放槍)。血量成長低的寵物偏攻擊(最高 ×1.25)、血厚的偏坦(×0.75)。黃金龍維持原樣。
- **函式/常數**:`PET_TIER_DMG_MULT`、`PET_HIT_TUNE`、`petDerive`(新增 `survivalDmgMult`/`damageMult`)、`petAttackOnce`、`petCastSkill`
- **規模**:~12 行 | **素材**:無 | **衝突**:無(我方 js/22 與 BASE 一致)
- **狀態**:✅ 已移植 @b52cb7d5e

### 1-2. 寵物共用桶「多分頁/多角色」同步大修 ⭐真 bug fix
- **玩家視角**:修掉「在別的角色身上剛給寵物換的裝備,切回來就不見了」「寵物出戰狀態被別的分頁洗掉」。寵物因魅力不足被自動收回時**會明講原因**(以前靜默收回 → 玩家回報「黃金龍自己不見了」)。
- **函式**:`_petNowStamp`、寵物物件新增 `eqV`/`outV` 版本戳、`_petMergeFromBucket`、`_petRosterResync`、`storage` 事件跨分頁合併、`petDeployToggle`(出戰互斥接手)、`_petEnforceCarry`(補說明訊息)
- **規模**:~95 行 | **素材**:無 | **衝突**:無
- **狀態**:✅ 已移植 @8467e5f9a(含 js/13 loadGame flush、js/11 開保管面板前 resync)

### 1-3. 召喚獸平衡:同階「低血高傷」曲線
- **玩家視角**:同一階的召喚怪,血少的打得比較痛、血多的比較耐打;食人妖精王 HP 270→400。
- **函式**:`_sumHpDpsMult`(新增)、`_sumDerive`、`SUMMON_TIERS` 資料一行
- **規模**:~10 行 | **素材**:無 | **衝突**:無
- **狀態**:✅ 已移植 @cb22102d3

### 1-4. 傭兵也能用召喚術/造屍術/屬性精靈 v2
- **玩家視角**:傭兵召的東西改用跟玩家一樣的 v2 公式(威力大幅拉齊),但**不上場、沒血條、不會被打**(抽象輸出)。
- **函式**:`mercSummonV2Plan`(js/23 新增)、`buildSummon` 的 `_v2form`/`_v2zmb` 分支、`refreshSummonBalance`(讀檔重算)、`summonAttack`(js/07);js/23 幾乎所有核心函式**加上 `owner` 參數**(原本硬讀 `player`)
- **規模**:~120 行(js/06 ~30、js/07 ~30、js/23 ~60)
- **衝突**:🟡 新增 2 個 `royalAllyMult()` 呼叫點 → 我方已停用(`return 1`),移植後是 no-op、不會壞,但上游那條平衡在我方無效
- **狀態**:✅ 已移植 @e327f6c8f。⚠ 移植時發現我方**先前漏搬 js/06+js/07 的召喚基礎設施**(`summonHitValue`/`summonDamageMult`/`_elfSpiritKingOverride`/`isMageSummonMastered`/`summonAttackCount` 完全不存在、`summonElementDamage` 少了魔抗穿透參數、無 `refreshSummonBalance`)→ **玩家的屬性精靈一攻擊就 ReferenceError、實際打不出傷害**;已於同 commit 一併補齊

---

## 🟢 第 2 組:低風險、可各自獨立挑

| # | 功能 | 玩家視角 | 涉及 | 規模 | 素材 | 衝突 | 狀態 |
|---|---|---|---|---|---|---|---|
| 2-1 | 擊殺/掉落訊息歸「玩家」 | 寵物/召喚補刀致死時,掉落訊息被日誌來源過濾吃掉 → 玩家看起來「沒掉東西」 | js/05 `killMob` `_combatSrc` 存還原 | 6 行 | — | 我方 tally 在 try 之前,不受影響 | ✅ @ede940fdf |
| 2-2 | 頭目狂暴 | 頭目血量低於門檻會暴走,命中與傷害同時提高(對傭兵也一樣) | `mobRageActive`/`mobRageHitBonus`/`mobRageDmgMult`(js/04);怪物欄位 `rageHpPct`/`rageHitMult`/`rageDmgMult` | ~25 行 | — | 無(沒設欄位的怪零影響) | ✅ @df98853b4(⚠ 目前**沒有任何怪帶這些欄位**——上游只給聖地雙 BOSS 設定,所以機制在位但玩家還看不到,要等聖地那批或我方自行設定) |
| 2-3 | 爆彈花蕊反噬改吃魔防與火抗 | 爆彈花蕊爆裂反噬自己的傷害不再硬吃全額 | js/04 `bombFlowerExplode` | 5 行 | — | 無 | ✅ @df98853b4(我方無傭兵版爆彈花蕊,只套玩家路徑) |
| 2-4 | 順移鈕改「優先用卷軸」 | 按順移時身上有瞬移卷軸就先用,沒卷軸才耗 MP(原本相反) | js/02 `playerTeleport` | ~10 行 | — | 無 | ✅ @0618eb578 |
| 2-5 | 副手互斥 | 副手武器(迅猛雙斧)與盾牌/臂甲不能同時裝,互相自動卸下 | js/08 `equipItem` | 13 行 | — | 無 | ✅ @cb97a2fa1 |
| 2-6 | 龍捲風動畫補回 | 施放龍捲風原本沒動畫 | js/09 `SPELL_FX` | 1 行 | 既有幀(assets/fx/龍捲風 20 張本來就在) | 無 | ✅ @df98853b4 |
| 2-7 | 負重顏色改 CSS class | — | js/01 `getLoadColor` | 2 行 | — | 🔴 **上游自己也沒有 `load-tone-*` 的 CSS**(全 repo grep 無) → 套用會讓負重數字失去顏色 | ❌ **刻意不移植**(維持現有 Tailwind 顏色) |
| 2-8 | 創角 BGM/音效殘留修正 | 進遊戲後登入 BGM 卡住、創角音效每 loop 重觸發 | js/13 `startGame`、js/17 `_bgmIsCreateScreen` | 10 行 | — | 我方創角動畫同區域,已一併加守衛 | ✅ @d9ca530bb |
| 2-9 | BOSS 降臨/狂暴特效 | BOSS 出場全屏閃光+衝擊波+名條;狂暴猩紅爆發 | js/09 `vfxBossEntrance`/`vfxBossRage`＋js/03 spawnMob 掛點＋css | ~150 行 | 無(純 CSS/JS) | 無 | ✅ @df98853b4(名單只留我方有的四大龍;上游的 `_vfxMute()` 對應我方 `_vfxOff()`) |
| 2-10 | 傷害飄字加「爆擊/重擊」標籤 | 爆擊/重擊的飄字前多一個小字標籤 | js/09 `_vfxNumber`＋css | ~15 行 | — | 無 | ✅ @df98853b4 |

---

## 🟡 第 3 組:中風險 / 需使用者拍板

### 3-1. 遺物第 15 批(8 件)
- 魔力阻抗襯衫(每 1 精神額外 MR)、**四大精靈王武器**(水=未命中 30% 改判擦傷 / 火=命中 10% 追加同等火魔傷 / 地=**受傷必定觸發一次連射** / 風=風屬性魔法 15% 免費追加龍捲風)、純潔少女的憐愛、破岩法師的秘術、將軍愛用的握劍護腕(持劍力量+N)
- **函式**:`missGrazeRate`(`getPhysicalDmg` **新增第 10 參數 `forceGraze`**,尾端追加、向後相容)、`hitEchoMagic`、`hurtRapidfire`(`rapidfireProc`/`allyRapidfire` **簽名擴充**)、`windSpellProcRate`、`swordStr`、`mrPerWis`
- **規模**:~35 行(跨 js/00/01/02/04/10) | **素材**:8 張 icon | **狀態**:✅ 已移植 @2026-07-17(使用者拍板要跟)
- 📌 移植註記:上游雖附 8 張專屬 icon,但這 8 件的 `img:` 欄位都指向既有基礎道具圖,而 `getIconUrl` 是 `img` 優先 → **上游那 8 張專屬圖其實不會被顯示**(上游自身的疏漏)。我方忠實照搬 `img`(顯示與上游一致),未搬那 8 張用不到的圖。要改用專屬圖的話是「移除 img 欄位」一行的事,待使用者決定。

### 3-2. 反叛者的盾牌
- 受到傷害時 1%(每強化 +2%)機率該次傷害 -50(物理魔法都算,傭兵也有)
- `shieldDmgReduceProc()`;玩家/傭兵 × 物理/魔法 共 4 個掛點 | ~14 行 | **狀態**:未移植(使用者此輪只挑了 3-1 與 3-3)

### 3-3. 無屬性抗性改制 `magicDrNonEle` → `resNone`
- **玩家視角**:紅騎士盾牌那類「無屬性魔法減傷」改成正式的「無屬性抗性」,走跟火/水/地/風一樣的遞減曲線。
- **⚠️ 這是純改動,會改變既有裝備的實際減傷數值。若不同步改 js/00-data 的裝備欄位,該裝備效果會直接失效歸零。**
- **規模**:6 行(含公式改寫)+ 跨 js/00/02/04/10 | **狀態**:✅ 已移植 @2026-07-17(使用者拍板要跟)
- 📌 移植註記:我方 js/03 `magicAttrDefense` 早就有 `resNone` 這個 key(隨魔法公式重寫進來)但 js/02 從沒設過該欄位 → 這條路一直讀到 undefined,這次補齊。三件裝備數值照上游不動(20/10/20)。顯示層刻意不照抄上游:能力面板用我方 HTML 結構、顯示 `effResistPct` 有效減傷%(上游顯示原始值)、不加會與數值列重複的遺物標籤。

### 3-4. 潘朵拉黑市權重規則重寫
- **玩家視角**:黑市能抽到的東西大改——以前「有價格就自動進池」,現在**只有明確標了 `gachaWeight` 或有在商店賣的東西才會出現**。
- 整支 `applyPandoraWeightRules()` 刪除;`initGachaWeights()` 重寫;fallback 由 100 → 0
- **⚠️ 對我方影響**:`afk-dex.js` 用 `gachaWeight>0` 判斷「潘朵拉限定物」→ 這條規則一改,判定結果整批位移,物品搜尋索引跟著變。
- **規模**:單檔淨 −65 行 | **狀態**:未移植 | **需拍板**

### 3-5. 裝備欄位解鎖等級調整(數值)
- 第 3 戒指 Lv55→**76**、第 4 戒指 Lv65→**81**、第 2 耳環 Lv50→**59**
- js/08 `equipItem` + js/10 `_rlv` 兩處同步 | 4 行 | **狀態**:未移植 | **需拍板**(等於變相削弱)

### 3-6. 傭兵設定記憶 + 換角自動解散 + 個別「解散」鈕
- **玩家視角**:①每個傭兵旁多一顆「解散」鈕(原本只能全員退出);②傭兵的喝水門檻與技能設定會被記住,再次招募自動套回;③存檔位被重建成新角色時,掛在身上的舊傭兵自動解散。
- `MERC_PREF_FIELDS`/`snapshotMercPrefs`/`applyMercPrefs`/`purgeReplacedAllies`/`dismissAlly`;新存檔欄位 `player.mercPrefs`
- **規模**:~85 行 | **衝突**:🔴 `dismissAlly` 用**原生 `confirm()`** → 我方有 `AFK_UI.confirm()` 規範,移植時要換掉;`purgeReplacedAllies()` 掛在 loadGame 尾端,**順序必須在我方 `offlineAfterLoad()` 之前**(否則離線結算會用到本該已解散的傭兵)
- **狀態**:未移植

### 3-7. 特效說明全面加料(E4/E5)
- **玩家視角**:裝備的特效說明從「只有名稱」→「名稱＋機率＋一句機制」;遺物專屬欄位有白話用途標籤(~60 個欄位對照)。
- `relicPurposeLabels()`(新函式 ~95 行)、`buildItemDescHTML` 的 `_eff` 重寫(~55 行)
- **與我方高度互補**:我方 2026-07-14 剛加了 `relicEffectLabels()`(帶精確數值),兩者可合併——**建議取上游的欄位覆蓋面 + 我方的精確數值原則**。
- **狀態**:未移植

---

## 🔴 第 4 組:大工程 / 高衝突

### 4-1. 黑暗妖精聖地整包
- **玩家視角**:新增 Lv48~99 的高階線。從新安全區「長老會議廳」的 NPC 進去,用「死亡騎士之書」開三張新地圖,打新怪與兩隻 Lv99 BOSS(**吉爾塔斯** HP 44 萬 / **真‧死亡騎士 冥皇丹特斯** HP 15 萬),掉「真．冥皇套裝」等新裝備。
- **內容**:
  - 地圖:`town_elder_council`(安全區,進選單)、`dark_elf_sanctuary`、`cursed_dark_elf_sanctuary`、`collapsed_elder_council_hall`(三張**不進地圖選單**,NPC 傳送進)
  - 怪物:9 隻新怪;兩隻 BOSS 帶 `noDmgCurve` 豁免旗標
  - **⚠️ 順帶把四大龍的傷害錨定到吉爾塔斯的 52.5%**(`anchorDragonSkill`/`anchorDragonDot`,~40 行)——**這會改動安塔瑞斯/法利昂/巴拉卡斯/林德拜爾的實際傷害,不是純新增**
  - 物品:~25 件(含真．冥皇 5 件套、受詛咒的真．冥皇執行劍=裝上變身死亡騎士、吉爾塔斯之劍/魔杖=擊殺後 10 秒增益)
  - 機制:**血壁空間**(BOSS 隨機獲得近/遠/魔法反射 10 秒,15 處掛點)、**BOSS 復活收費**(每次復活扣 1 本書,沒書被踢出)、**完整召喚球保 HP**(離開時消耗,BOSS 帶傷)
- **規模**:~300~350 行(跨 js/00/01/02/03/04/05/11/13/14/17)
- **素材**:約 830 檔(怪物動畫 797、區域背景 4、BGM 2、音效 6、NPC sprite 2、物品圖示 ~20)
- **🔴 高衝突**:
  1. **BOSS 復活收費插在 `tick()` 的出怪迴圈裡(含 `break`)——正是我方抽出成 `maybeSpawnMobs()` 的那段**。移植時要改放進 `maybeSpawnMobs()`。
  2. **離線快速結算會直接呼叫 `maybeSpawnMobs()`** → 等於離線時也會扣書、扣光後把玩家傳送出去。**掛一晚會把玩家的書全部燒光。** 建議三張聖地圖比照排名/裂痕,走「不續、不結算」或至少純 BOSS 房排除快速段。
  3. `giltasKeepOnLeave()` 插在 `changeMap` 的 `mapState.current` 賦值前,我方 `offlineStamp()` 在函式尾端 → 可共存但 3-way merge 會撞。
  4. `AFK_EXTRA.mapName` 必補 4 個 id,否則我方 smoke 會 exit 1(這是好事,會擋下來)。
- **狀態**:✅ 已移植 @2026-07-16。移植決策:①四大龍傷害**已依用戶拍板重新錨定成吉爾塔斯 52.5%**(另補 rollAverage/setAverageRoll helper·未引進整套壓縮曲線);②離線**只排除兩間 BOSS 房**(cursed/collapsed → js/offline.js maybeCatchup skip;一般狩獵圖 dark_elf_sanctuary 照常離線續掛;sanctBossRespawnCharge 另加 state.ff 雙保險不燒書)。跨檔:js/00(items/mobs/town/mapSpawns/dragon-anchor)、js/01(drops/weights/MEM/AOE)、js/02(emperor set/cursed blade poly/giltas wand extraMp/doTeleport hook)、js/03(book charge/mag4/PURE_BOSS/giltas fury/orb HP/qigu reflect)、js/04(reflectWallOnDamage+shieldDmgReduceProc+8 hooks/alsoPierce)、js/05(giltas fury write/giltasKeepOnLeave)、js/06(10 傭兵 reflectWall hooks+giltas fury)、js/07(6 玩家技能 reflectWall hooks)、js/09(MOB_ANIM_NAMES/SPRITE_SHADOW+8/BOSS_ENTRANCE_FX)、js/10(cursed blade WEAPON_TAGS)、js/11(town/region/indicator/dantes gate/atelier/changeMap hook)、js/13(area+town bg)、js/14(atelier recipes)、js/17(giltas/dantes sfx+boss BGM)、offline.js(skip 2 BOSS 房)、afk-extradata(mapName)、afk-wiki(真．冥皇套裝 SETS)。素材:9 怪 anim+npc 5454+3 area bg+town bg+music153/162+sfx 1992-1995/91+26 icon,manifests 已重產。Playwright 全驗:0 console error、狩獵/BOSS 出怪、書扣費(3→2)、giltas fury、reflectwall(直呼/mag4 自 buff/玩家 hook/傭兵 hook)、NPC gate 進圖、cursed blade 變身、emperor 5 件套、giltasKeep 保 HP。**尚待(可跑 /update-wiki 批次補):小百科散文——血壁空間/頭目狂暴/BOSS 復活收費 進「戰鬥機制」頁、聖地 3 隱藏圖進「地圖」頁(hidden 圖不在 MAP_CATEGORIES)。掉落查詢已全自動涵蓋(data-driven)。**

### 4-2. 裝備視窗改成分頁內嵌 + 格線 4×8 → 4×6
- **玩家視角**:「裝備」分頁不再是清單,而是一張人形裝備圖,欄位直接標在角色圖上(每個職業/性別各有專屬底圖)。
- `setEquipmentPanelEmbedded()`、`PAGE_SLOTS` 全面重排(第 1 頁 19 格、第 2 頁 8 格席琳遺骸)、負重顯示
- **規模**:js/19 182 行 + index.html + floating-ui.css 110 行 + js/10 ~25 行 | **素材**:16 張(男/女 × 8 職業)+ 3 張 ui
- **🔴 衝突**:我方 `renderTabs` 用 `{ sep: '🦴 席琳遺骸' }` 分隔列,上游用 `_padCells` 把遺骸推到第 25~32 格 → **hunk 直接衝突,要重寫不是套用**;我方倉庫的「席琳遺骸」分類也綁這組資料。
- **狀態**:未移植

### 4-3. 能力頁整頁重做
- 圖片式版面,新增六個新數值(近/遠/魔法爆擊傷害、藥水恢復量、移動速度、擊殺回魔、魔防、無屬性抗性)
- **規模**:~220 行(index.html + css + js/08) | **素材**:1 張
- **🔴 衝突**:**會撞 `afk-statpts.js`**(我方的「點數來源分解」靠 `#dt-<stat>` 插一行,新版改成絕對定位在圖上的固定 % 座標 → 插一行會跑版)。移植 = afk-statpts 必須改寫或停用。
- **狀態**:未移植

---

## ✗ 不適用(我方沒有這個系統)

### 城鎮 NPC 站位重排
- 上游依 1920×1080 背景圖逐城重排 `TOWN_NPC_SPOTS`(~40 行)、NPC 火焰疊層、NPC 影子 fallback。
- **我方 repo 完全沒有「城鎮 NPC 地圖化」系統**(`TOWN_NPC_SPOTS`/`renderTownNPCMap`/`NPC_SPR` 一個都沒有)→ **整包跳過**。
- 素材 ~75 檔也不必搬。

---

## 我方「刻意偏離」處的衝突總表

| 我方偏離點 | 上游這批有沒有動到 | 說明 |
|---|---|---|
| **`maybeSpawnMobs` 自 tick 抽出** | 🔴 **有,直接命中** | 聖地 BOSS 復活收費插在 tick() 出怪迴圈內。且離線快速結算會走到 → 會燒光玩家的書。 |
| **`js/05 killMob` 的離線 tally** | 🔴 有 | G8 把整段包進 try/finally,要確認我方 `__afkKillTally` 留在 try 內 |
| **`js/13 loadGame` 的 `offlinePreLoad`/`offlineAfterLoad`** | 🔴 有 | `petRosterSave()` flush 插在函式第一行;`purgeReplacedAllies()` 插在尾端,**順序必須在 `offlineAfterLoad()` 之前** |
| **`js/11 changeMap` 的 `offlineStamp`** | 🟡 有 | `giltasKeepOnLeave()` 插在 `mapState.current` 賦值前,可共存但會撞 merge |
| **`royalAllyMult`(我方 `return 1`)** | 🟡 新增 2 個呼叫點 | 移植後 no-op,不會壞,但上游平衡在我方無效 |
| **`_isMercSelfBuff`(我方修過 bug)** | 🟢 未動 | 我方修正可完整保留 |
| **`_pmAbsorbSelfHpCost` / `_PM_ATTACK_SKILLS`** | 🟢 未動 | 無衝突 |
| **`equipItem(item,silent)` / `useItem` keepModal** | 🟢 未動 | 相容 |
| **`_trimLog` 捲動錨定** | 🟢 未動 | — |
| **`saveOnExit` 去重** | 🟢 未動 | — |
| **js/12 倉庫(我方大改)** | 🟢 **完全未動** | 零衝突 |
| **自動販賣 / `_craftEquipNeeds`** | 🟢 **完全未動** | 零衝突 |
| **`afk-statpts.js`** | 🔴 被能力頁重做撞到 | 見 4-3 |
| **`afk-mobile.js` 的 `#mv-action-btn`** | 🔴 上游把這顆鈕刪了 | 我方手機回村依賴它 → 移植 js/08 `_updateUIImpl` 時要保留 |
| **`afk-dex.js` 的 `gachaWeight>0`** | 🟡 被潘朵拉權重改制影響 | 見 3-4 |

---

## 建議優先順序

1. **第 1 組(寵物/召喚獸 v2 後續)** — 直接補我方剛移植的兩個系統的洞,不跟會踩到上游已修的 bug。
2. **第 2 組低風險小修** — 幾行到幾十行,獨立、玩家有感。
3. **第 3 組** — 需拍板的平衡改動(潘朵拉權重、欄位解鎖等級、無屬性抗性)。
4. **第 4 組** — 大工程,尤其黑暗妖精聖地要先解掉「離線續掛燒書」的問題。
5. **城鎮 NPC 站位** — 我方沒這系統,不做。
